<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @category.description %>
</p>

<p>
  <strong>Menu:</strong>
  <%= @category.menu.title %>
</p>
<% @category.products.each do |products| %>
  <%= products.name %>
<% end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
<% @category.products do |p| %>
    <li><%= p.name %></li>
<% end %>
